# ft2.factor 因子挖掘模块依赖文件
# 
# 安装说明：
# 核心依赖：pip install -r requirements.txt
# 完整依赖：pip install -r requirements.txt && pip install -r requirements-optional.txt

# ==================== 核心依赖 ====================
# 数据处理和科学计算
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0

# ==================== 可选依赖 ====================
# 可视化支持（用于生成IC序列图、分组收益图等）
matplotlib>=3.5.0

# 机器学习支持（用于PCA正交化、标准化等）
scikit-learn>=1.1.0

# YAML格式支持（用于因子库的YAML格式导入导出）
PyYAML>=6.0

# ==================== 说明 ====================
# 
# 1. 核心依赖是必须的，模块的基本功能需要这些库
# 2. 可选依赖提供了额外的功能：
#    - matplotlib: 用于生成因子检验的可视化图表
#    - scikit-learn: 用于PCA正交化和标准化处理
#    - PyYAML: 用于YAML格式的因子库导入导出
# 3. 如果不需要这些额外功能，可以只安装核心依赖
# 4. 所有依赖都兼容Python 3.8+
